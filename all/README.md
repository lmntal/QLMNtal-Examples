## all

### 中間命令列への直し方

### examples

- atoms
- link
- mem
  - [x] c(L1),c(L2),c(L3), {a(L1),a(L2),a(L3)}. {<+>a(X),$p} :- {<+>b(X),$p}.
    - -> c(L1),c(L2),c(L3), {b(L1),b(L2),b(L3)}.
    - 膜の中にある1個以上のa(X)を全て書き換える
- nest
